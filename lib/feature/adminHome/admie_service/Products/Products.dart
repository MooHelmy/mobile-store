import 'package:flutter/material.dart';

import 'widget/Proudcts_body.dart';

class Poducts extends StatelessWidget {
  const Poducts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Products_body(),
    );
  }
}
